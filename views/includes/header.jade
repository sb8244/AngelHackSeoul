.navbar.navbar-fixed-top(style='position: absolute;')
    .navbar-inner
        .container(style='width: auto; padding: 0 20px;')
          a.brand(href='/') Popup
          ul.nav.navbar-fixed-top.pull-right
            ul.nav.pull-right
              li
                a(href='/register' ) Sign up
              li.dropdown
                a(class="dropdown-toggle", id="signin", href="#", data-toggle="dropdown") Sign In
                .dropdown-menu(style="padding:15px;")
              //
                 Button to trigger modal 

div.modal.hide(id="signin")
    .modal-header
        button(type="button", class="close", data-dismiss="modal") x
        h3 Modal Header
    .modal-body
        p One fine body...
    .modal-footer